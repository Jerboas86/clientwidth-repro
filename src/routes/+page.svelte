<script>
  let width = 100;
  let height = 100;
</script>

<div bind:clientWidth={width}>CSP violation with bind:clientWidth</div>
<div bind:clientHeight={height}>CSP violation with bind:clientWidth</div>
